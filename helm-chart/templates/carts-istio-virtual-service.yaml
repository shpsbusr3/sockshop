apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
    name: sockshop-staging-carts
spec:
    hosts:
        - carts.sockshop-staging.199.223.233.63.xip.io
    gateways:
        - sockshop-staging-gateway
    http:
        -
            route:
                -
                    destination:
                        host: carts.sockshop-staging.svc.cluster.local
                        subset: blue
                    weight: 100
                -
                    destination:
                        host: carts.sockshop-staging.svc.cluster.local
                        subset: green
                    weight: 0
